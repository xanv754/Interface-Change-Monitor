---
const token = Astro.cookies.get("token");
const userType = Astro.cookies.get("type");

if (token) Astro.cookies.delete("token");
if (userType) Astro.cookies.delete("type");
---
<main class="w-full h-full flex justify-center items-center">
    <div class="h-fit w-40 px-4 py-8 bg-white rounded-xl flex flex-col items-center">
        <img src="/assets/logo.png" alt="Logo ICM" class="h-20 w-20">
        <h2 class="font-lexend font-bold text-blue-500 text-3xl mb-2">Sesión Expirada</h2>
        <p class="font-lexend text-blue-500 px-5 text-center mb-3">Por seguridad, tu sesión ha sido cerrada. Inicia de nuevo si quieres continuar navegando.</p>
        <button id="expired" class="rounded-full font-lexend font-bold text-white bg-blue-500 px-6 py-2 transition-all duration-300 ease-in-out hover:bg-blue-800">Aceptar</button>
    </div>
</main>
<script>
    const button = document.getElementById("expired");

    if (button) {
        button.addEventListener('click', () => {
            location.href = "/";
        })
    }
</script>
